.message{data: {message: {id: message.id}}}
  .upper-message
    %p.upper-message__user-name__talker
      = message.user.name
    %p.upper-message__date
      = message.created_at.strftime("%Y-%m-%d(%a)")
  %p.lower-message__content
    = message.content
       
